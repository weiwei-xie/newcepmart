server:
  port: 8080
eureka:
  instance:
    hostname: eureka-center
    appname: eureka-center-app
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8080/eureka
  server:
    enableSelfPreservation: false
    evictionIntervalTimerInMs: 4000